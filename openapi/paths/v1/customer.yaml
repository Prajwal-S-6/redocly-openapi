get:
  operationId: getAllCustomers
  tags:
    - Customer
  summary: Get all customers
  description: List of Customers
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/v1/CustomersList.yaml'
    '400':
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/Problem.yaml'
